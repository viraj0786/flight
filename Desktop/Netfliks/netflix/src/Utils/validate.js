export function valiDate(email , password , name = "viraj@fd.fdfd") {
   // let nameRegx = name &&  /[a-z0-9]+@[a-z]+\.[a-z]{2,3}/.test(name);
   // let emailRegx = /[a-z0-9]+@[a-z]+\.[a-z]{2,3}/.test(email);
   // let passwordRegx = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@.#$!%*?&])[A-Za-z\d@.#$!%*?&]{8,15}$/.test(password);

   // if(!nameRegx){ return "Name Is Not Valid"}
   // if(!emailRegx){ return "Email Is Not Valid"}
   // if(!passwordRegx){ return "Password Is Not Valid"}

   return null;
  }